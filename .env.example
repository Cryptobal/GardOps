# URL de conexión a Postgres (Neon)
POSTGRES_URL="postgres://user:password@host/db?sslmode=require"

# Nota: En local, los endpoints aceptan el header "x-user-email" como fallback para identificar usuario.
# En producción, usar sesión/JWT.


